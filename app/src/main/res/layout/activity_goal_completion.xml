<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#f4f4f4"
    tools:context=".GoalCompletionActivity">

    <!-- Back Button -->
    <Button
        android:id="@+id/btnBack"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:backgroundTint="#FF3D00"
        android:text="BACK"
        android:textColor="#FFFFFF" />

    <!-- Title -->
    <TextView
        android:id="@+id/txtCompletedGoalsTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp"
        android:text="Completed Goals"
        android:textColor="#000000"
        android:textSize="24sp"
        android:textStyle="bold" />

    <!-- Scrollable List -->
    <ScrollView
        android:id="@+id/scrollCompletedGoalsList"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/txtCompletedGoalsTitle"
        android:layout_marginTop="16dp">

        <LinearLayout
            android:id="@+id/layoutCompletedGoalsContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp" />

    </ScrollView>

</RelativeLayout>
